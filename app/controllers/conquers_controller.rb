class ConquersController < ApplicationController

	# def update
	#  	# Create Activities
 #    # --> Goes out to every User that subscribed to the Player that Conquered (new_player_id)
 #    if @owner = Player.find(params[:new_player_id]).present?
 #      if @owner.followers.count > 0
 #        @conquer.create_activity :create, recipient: @owner.followers
 #      end
 #    end
	# end

end
